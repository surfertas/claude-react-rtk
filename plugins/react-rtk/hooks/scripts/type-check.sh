#!/usr/bin/env bash
# Type checking is deferred to /rx:work phase checkpoints and /rx:verify.
# Per-file tsc --noEmit is too slow and unreliable (cross-file dependencies).
# This stub exists for future expansion.
exit 0
